// O() 
/*---------------- WIP --------------*/
function nextPermutation(num) {
    const arr = (num + '').split('').slice(1,)
    const sorted = arr.sort((a, b) => b - a);
    let next = 0;
    for (let i = 0; i < arr.length; i += 1) {
        // if (arr[0] )
    }
/* --------------- WIP --------------*/
}
console.log(nextPermutation(48879, 4999))